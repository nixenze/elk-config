- pipeline.id: system_mesos-log
  path.config: "/etc/logstash/conf.d/mesos*.conf"
  queue.type: persisted

- pipeline.id: applog
  path.config: "/etc/logstash/conf.d/app*.conf"
  queue.type: persisted
